syntax = "proto3";
package chat;

message Message{
    string body=1;
}

message Response {
    string info = 1;
    string name = 2;
    int32 elegido = 3;
    int32 asignado =4;
    bytes fileChunk = 5;
    
}

service ChatService{
    rpc SayHello(Response) returns (Message){};
}