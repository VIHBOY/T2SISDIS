syntax = "proto3";
package chat;

message Message{
    string body=1;
}

message Response {
    string info = 1;
    bytes fileChunk = 2;
}

service ChatService{
    rpc SayHello(Response) returns (Message){};
}